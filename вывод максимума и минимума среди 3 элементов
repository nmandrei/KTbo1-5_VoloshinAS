#include <stdio.h>

int main()
{
	int a, b, c;
	scanf_s("%d%d%d", &a, &b, &c);
	int k = a > b ? a : b;
	int max = c > k ? c : k;
	int l = a < b ? a : b;
	int min = l < c ? l : c;
	printf("%d %d", min, max);
	return 0;
}
// вывод максимума и минимума среди 3 элементов
